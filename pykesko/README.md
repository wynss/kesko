### PyKesko

PyKesko is the python package for the Kesko simulator. It contains python bindings to the Rust code providing an API to interact with simulator. It also contains 

**## Getting Started
### Prerequisites

Make sure you have one of the supported python versions installed. It is also recommended to use a virtual environment.

### Installation

Just run
```bash
pip install pykesko

```

## Usage
### Run the simulator

### Use Gym environment

### Use Stable Baselines3 to train an RL agent to walk
